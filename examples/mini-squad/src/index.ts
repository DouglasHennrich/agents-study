/**
 * mini-squad — orquestrador de agents inspirado em bradygaster/squad.
 *
 * Esta entrada existe apenas para que `tsc` tenha algo para compilar
 * antes da Phase 3. Será populado nas próximas phases.
 */

export const VERSION = '0.1.0';
